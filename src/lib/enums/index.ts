import * as messages from '../enums/lib.enum.messages';
import * as labels from '../enums/lib.enum.labels';
import * as status from '../enums/lib.enum.status';

export default {
    ...messages,
    ...labels,
    ...status
}